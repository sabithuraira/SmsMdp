<p id="notice"><%= notice %></p>
<p><i>Absensi dan nilai dapat dilihat pada informasi detail kelas</i></p>
<div class="row">
  <div class="col-xs-12">
    <div class="box">
      <div class="box-header">
        <h3 class="box-title">Kelola Kelas</h3>
        
        <%= link_to raw('<i class="fa fa-plus"></i> Add New'), new_kela_path, class: "btn btn-box-tool btn-default" %> 
        <div class="box-tools">
          <%= form_tag({controller: "kelas", action: "search"}, method: "post", id: "form_index") do %>
            <div class="input-group input-group-sm" style="width: 150px;">
              <input type="text" id="table_search" class="form-control pull-right" placeholder="Search">

              <div class="input-group-btn">
                <button type="submit" id="index_data" class="btn btn-box-tool btn-default"><i class="fa fa-search"></i></button>
              </div>
            </div>
          <% end %>
        </div>
      </div>
      <!-- /.box-header -->
      <div class="box-body table-responsive no-padding">
        <table class="table table-hover" id="table_data">
          <tr>
            <th>Dosen</th>
            <th>Mata pelajaran</th>
            <th>Tahun ajaran</th>
            <th>Nama</th>
            <th colspan="4"></th>
          </tr>

         
          <% @kelas.each do |kela| %>
            <tr>
              <td><%= kela.dosen_rel.name %></td>
              <td><%= kela.mata_pelajaran_rel.name %></td>
              <td><%= kela.tahun_ajaran %></td>
              <td><%= kela.nama %></td>
              <td><%= link_to 'Show', kela %></td>
              <td><%= link_to 'Edit', edit_kela_path(kela) %></td>
            </tr>
          <% end %>
        </table>
      </div>
      <!-- /.box-body -->
    </div>
    <!-- /.box -->
  </div>
</div>

<%= javascript_include_tag 'themes'%>